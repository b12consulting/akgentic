id: id_agent_limits_member
kind: model
namespace: prdf-dimensions-v22
user_id: anonymous
model_type: akgentic.llm.AgentUsageLimits
description: 'Lifetime budget for a member: 50 runs, 500k tokens. Bounds the whole
  session even if every individual run stays under its own cap.'
payload:
  agent_request_limit: 50
  total_tokens_limit: 500000
