id: skeptic
kind: agent
namespace: general-team
user_id: anonymous
model_type: akgentic.core.AgentCard
description: Holds the adversarial stance — tries to break an answer before anyone
  relies on it. Hunts counter-examples, hidden assumptions and statistics doing too
  much work. Can search to refute; cannot fetch pages and has NO shell.
payload:
  description: Holds the adversarial stance — tries to break an answer before anyone
    relies on it. Hunts counter-examples, hidden assumptions and statistics doing
    too much work. Can search to refute; cannot fetch pages and has NO shell.
  agent_class: akgentic.agent.BaseAgent
  skills:
  - critical-thinking
  - counter-examples
  - assumption-hunting
  - refutation
  - web-search
  - writes-own-note
  - no-shell
  config:
    name: '@Skeptic'
    role: Skeptic
    prompt:
      __ref__: id_skeptic_prompt
    model_cfg:
      __ref__: global.id_gpt_52
    run_usage_limits:
      __ref__: id_run_limits_member
    agent_usage_limits:
      __ref__: id_agent_limits_member
    tools:
    - __ref__: id_search_skeptic
    - __ref__: id_workspace_note
    - __ref__: id_knowledge_graph_ro
    - __ref__: id_team_member
    - __ref__: id_vector_store
