id: quant
kind: agent
namespace: general-team
user_id: anonymous
model_type: akgentic.core.AgentCard
description: Holds a Python sandbox and a working directory — computes rather than
  recalls.
payload:
  description: Holds a Python sandbox and a working directory — writes scripts, runs
    them, computes, estimates, simulates and checks the arithmetic. Turns "roughly?"
    into a number with its assumptions on the table. Can write files and run code;
    has NO web access.
  agent_class: akgentic.agent.BaseAgent
  skills:
  - computation
  - estimation
  - fermi-problems
  - simulation
  - shell-exec
  - python
  - workspace-write
  - workspace-edit
  - no-web
  config:
    name: '@Quant'
    role: Quant
    prompt:
      __ref__: id_quant_prompt
    model_cfg:
      __ref__: global.id_gpt_52
    run_usage_limits:
      __ref__: id_run_limits_member
    agent_usage_limits:
      __ref__: id_agent_limits_member
    tools:
    - __ref__: id_exec
    - __ref__: id_workspace_quant
    - __ref__: id_knowledge_graph_ro
    - __ref__: id_team_member
    - __ref__: id_vector_store
