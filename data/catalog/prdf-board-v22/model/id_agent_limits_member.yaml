id: id_agent_limits_member
kind: model
namespace: prdf-board-v22
user_id: anonymous
model_type: akgentic.llm.AgentUsageLimits
description: 'Lifetime budget for a member: 40 runs, 300k tokens. Bounds the whole
  session even when every individual run stays under its own cap — the failure mode
  a per-run limit cannot see.'
payload:
  agent_request_limit: 40
  total_tokens_limit: 300000
