id: human_proxy
kind: agent
namespace: insurance-call-center-v2
user_id: anonymous
model_type: akgentic.core.AgentCard
description: Human caller interface — primary entry point
payload:
  description: Human caller interface — primary entry point
  agent_class: akgentic.agent.HumanProxy
  skills: []
  config:
    name: '@Human'
    role: Human
